// import { useQuery } from "react-query";/
import MainApp from "./components/MainApp";

const App: React.FC = () => {
  // const query = useQuery({
  //   queryKey: "advise",
  //   queryFn:
  // });
  return (
    <div className="  ">
      <MainApp />
    </div>
  );
};

export default App;
